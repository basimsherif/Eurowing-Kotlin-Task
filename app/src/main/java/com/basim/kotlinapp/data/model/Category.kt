package com.basim.kotlinapp.data.model

/**
 * Class which provides a model for category
 * @constructor Sets all properties of the category
 * @property title title of the category
 */
data class Category(val title : String)